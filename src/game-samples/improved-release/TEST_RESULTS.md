# 검증 결과 — 개선 릴리스

기준은 최초 로컬 체크포인트 `0162756`이다.
과거 최초 실행 기록은 변경 없이 `FIRST_RELEASE_RESULTS.md`에 보존했다.
그 문서의 first-release 경로·실행 시각·실패·결과는 역사적 증거이며 이번 개선판의 실행 결과가 아니다.

## 환경·대상

- 실행일: 2026-09-13, 한국 표준시. 최종 환경·산출물 확인 시각 `2026-09-13T23:20:44+09:00`.
- 작업 루트: `src/game-samples/improved-release`.
- 최초 비교 기준: `016275624ba0b624319e0947dc7db2cf462c9928`.
- 실행 당시 저장소 HEAD: `7328797d003093ca362b19fc8fe5cff20db72c03`.
- 검증 대상: 위 HEAD에 아직 커밋하지 않은 **개선 샘플 전체**. 두 SHA 모두 배포 SHA가 아니다.
- Windows, Node.js `v24.14.1`, npm `10.8.3`, Vite `8.2.2`, Playwright `1.63.0`.
- Chromium `153.0.8010.12`. 기존 설치를 사용했으며 이번에는 브라우저 설치 명령을 실행하지 않았다.
- 최초의 정확한 패키지·잠금 파일을 유지했다. npm 출력의 `orbit-defender-first-release@1.0.0`은 보존된 manifest 이름이며 실행 경로는 개선 폴더다.
- E2E는 Chromium 1개 프로젝트, worker 1개, 재시도 0회. 시계는 Playwright로 제어했으며 운영 상태 주입은 없다.

## 실제 실행 순서·결과

처음 CHANGE_REQUEST와 영향 문서를 작성할 때 구현·검증은 미수행으로 기록했다.
그 뒤 A 규칙을 구현·검증하고 B 브라우저 작업을 이어서 진행했다. 최종 명령은 아래 순서를 지켰다.

| 단계 | 명령 | 실제 출력·판정 | 종료 코드 |
|---|---|---|---|
| A 준비 | `npm ci` | 새 폴더에 의존성이 없어 잠금 파일로 18개 설치·19개 감사, 알려진 취약점 0개 | 0 |
| A | `npm test` | 42개 통과, 실패·취소·skip 0개, 279.841ms | 0 |
| 최종 1 | `npm test` | **42개 통과**, 실패·취소·skip 0개, 236.6729ms | 0 |
| 최종 2 | `npm run test:e2e` | **13개 통과**, 실패 0개, 출력 기준 1.3분 | 0 |
| 최종 3 | `npm run build` | Vite 8.2.2, 모듈 6개, dist 3파일, 346ms | 0 |
| 최종 4 | `node scripts\check-build.mjs` | `/improved-release/`, 로컬 요청 3개·오류 0개, playing·0점 시작 및 P 정지·재개 통과 | 0 |

최종 순차 실행 명령:

```powershell
npm test && npm run test:e2e && npm run build && node scripts\check-build.mjs
```

이번 개선 검사에서는 실패가 없었다. 재시도·테스트 삭제·기대값 완화로 실패를 숨기지 않았다.
최초 릴리스의 Chromium 누락·선택자 실패는 과거 기록에 그대로 보존했다.
이번에는 해당 과거 실패를 다시 발생한 것으로 기록하거나 개선판 결과로 재사용하지 않았다.
결과 문서 갱신 중 patch의 문맥 불일치는 파일 내용을 다시 확인한 뒤 해당 문서만 재적용해 해결했다.
코드나 검사 결과에는 영향이 없었다.

## CHG-01 기준별 실제 근거

| 기준 | 상태 | 실제 assertion·구현 근거 |
|---|---|---|
| AC1 | 통과(pass) | TC-P01 Node: playing/paused만 전환, 반복 P·keyup·title/won/lost 무시. E2E에서 제목과 자연 승패 화면 P 무시·반복 keydown 유지 확인 |
| AC2 | 통과(pass) | TC-P02 Node: 발사 후 0.1초 상태에서 dt=0/0.1/60/3600 뒤 전체 깊은 값과 위치·적·탄환·점수·쿨다운·시간·번호 동일. E2E 60초 이상 시계 진행 뒤 원본 Canvas 데이터와 표시 버퍼 동일 |
| AC3 | 통과(pass) | TC-P05 E2E: “일시정지” 상태·P 안내, 보이는 버튼 0개, 재개 overlay 숨김. 이후 자연 승패의 제목·재시작 표시 정상 |
| AC4 | 통과(pass) | TC-P03/07 Node: 전환 키 비우기·paused 입력 무시·인자 불변. E2E Space/방향 유지·반복 후 잔류 없음, 다시 눌러야 이동·발사. playing/paused blur/focus는 상태 유지 |
| AC5 | 통과(pass) | TC-P04 Node: 정지 60초 후 남은 0.1초 쿨다운, 경계 1e−10초 직전 재발사 금지·정확한 경계 발사. E2E 긴 정지 후 첫 16ms의 RAF에서 Canvas 데이터 동일 |
| AC6 | 통과(pass) | TC-P06 E2E: 10회 정지/재개마다 픽셀 동결·첫 프레임 동일. 이후 0.5초 이동 약 160픽셀, 96ms 후 탄환 1개·추가 128ms 후 2개. 기존 단일 RAF 예약 경로 재사용 |
| AC7 | 통과(pass) | 최초 35개 Node 테스트 본문이 그대로 남고 모두 통과. 기존 10개 E2E에 경계 assertion만 추가, 전체 13개 통과. 빌드·상대 경로·자연 승패·재시작 유지 |

`src/game.js`에서 기존 repeat 차단 뒤 P 전환을 추가하고 `clearInput`을 재사용했다.
기존 `step`의 playing 이외 상태 반환이 paused에서도 시간·쿨다운을 진행하지 않는다.
`src/main.js`의 기존 `accept`는 **모든 상태 전환**에서 입력과 `previousTime`을 초기화한다.
RAF는 최초 부팅에서 1회 예약하고 콜백 끝에서 다음 1회만 예약한다.
정지·재개 핸들러에는 RAF 예약을 추가하지 않았다. 반복 전환 뒤 실제 이동·발사량으로도 회귀 확인했다.

## 기존 REQ-01~08 회귀

| 요구사항 | 상태 | 이번 실행 근거 |
|---|---|---|
| REQ-01 | 통과(pass) | 제목 입력 제한, 버튼/Enter 시작, 0점, 제목의 P 무시 |
| REQ-02 | 통과(pass) | 네 방향 키·동시 키·x=0/760·y=550, 재개 뒤 픽셀 이동량 |
| REQ-03 | 통과(pass) | 첫 발·0.2초 경계·Space 유지·화면 밖 제거, 정지 중 남은 쿨다운 보존 |
| REQ-04 | 통과(pass) | 24개 좌표, 양쪽 편대 경계·단일 하강·죽은 적 제외 |
| REQ-05 | 통과(pass) | 양의 AABB·접촉 제외·단일 명중·중복 득점 없음 |
| REQ-06 | 통과(pass) | 520 경계·충돌 우선·종료 동결. E2E 정상 키 조작 승리 240점, 자연 하강 패배 |
| REQ-07 | 통과(pass) | 두 종료 상태의 전체 초기화, 자연 패배 R/버튼 반복·승리 버튼 Enter, 재시작 직후 발사 |
| REQ-08 로컬 | 통과(pass) | 접근 가능한 상태·점수·버튼, 좁은 화면 비율, 외부 요청 없음, dist 상대 에셋·P 동작 |
| REQ-08 원격 | 미확인(unverified) | 실제 Actions/Pages 권한·환경·공개 URL은 범위 밖 |

## 산출물·화면 증거

최종 dist 실제 파일:

| 파일 | 바이트 |
|---|---|
| `index.html` | 3,077 |
| `assets/index-Di7bJJ73.css` | 3,016 |
| `assets/index-BBwU-OTk.js` | 7,019 |

빌드 검사는 dist 3파일만 별도 서버로 제공하며 상대 JS/CSS와 P 정지·재개 assertion을 실행했다.
빌드에 문서·테스트·node_modules를 포함하지 않는다.

**정지 화면 PNG**:

`test-results/game-TC-P02-05-긴-정지의-화면-동결·반복-P·재개-첫-프레임·잔류-입력을-확인한다-chromium/paused.png`

크기 160,217바이트. 실제 E2E에서 생성했으며 테스트 폴더 아래에 남아 있다.
Git에서 제외된 생성물이므로 새 checkout에는 없고 E2E 재실행 시 생성된다.
부모 작업자가 문서용으로 별도 보존할 수 있으며 이 작업에서는 상위 에셋 폴더를 수정하지 않았다.
제목·플레이·승리 캡처도 이번 E2E가 개선 화면으로 다시 생성했다.

## 원본 보존·차이 검증

- `git diff 0162756 -- src/game-samples/first-release`에 출력이 없었다. 최초 소스는 변경하지 않았다.
- `ideation.md`, `package.json`, `package-lock.json`, `src/styles.css`, `vite.config.js`,
  `playwright.config.js`, `.github/workflows/pages.yml`은 원본과 파일 해시가 같았다.
- 개선 Node 테스트 파일은 원본 테스트 파일의 전체 본문으로 시작함을 확인했다. 기존 35개 검사를 삭제·수정하지 않았다.
- `FIRST_RELEASE_RESULTS.md`의 해시는 최초 `TEST_RESULTS.md`와 같았다.
- 새 CHANGE_REQUEST와 기록 분리 외에는 CHG-01 영향 파일만 갱신했다.
- E2E 종료 후 포트 5173의 LISTEN 연결 수는 0이었다. 충돌·타인 서버 종료·기존 서버 재사용은 없었다.

## 수행 차이·미확인·회고

07-01~05를 읽고 변경 요청 → 필요한 문서 → A 규칙 → B 브라우저 → 전체 검증 → 결과 기록 순서를 따랐다.
다만 실제 배포를 전제로 하는 원격 동기화·PR·병합·재배포와 참가자별 승인·커밋 단계는 수행하지 않았다.
기준은 로컬 `0162756`이며 배포 증거를 만들어내지 않았다.

`ideation.md`는 최초 선택 배경이어서 그대로 유지했다. `AGENTS.md`는 “최초만” 경계가 새 요청과 충돌하므로
해당 범위·중단 문장과 변경 요청 참조만 조정하고 승인·비밀정보·기존 변경 보호·검증 규칙은 보존했다.
가장 유용한 기준은 “정지 후 남은 쿨다운”과 “재개 첫 프레임”이었다.
단순 정지 화면 검사만으로 시간 점프·잔류 입력·중복 RAF를 놓치지 않도록 값과 픽셀을 함께 확인했다.

남은 미확인: 인간의 실제 시간 플레이, 실제 OS 창 전환, Copilot app UI·자동 지침 적용,
다른 브라우저, GitHub Actions·Pages 공개 재배포. E2E의 합성 blur/focus를 OS 조작 성공으로 주장하지 않는다.
커밋·푸시·배포를 하지 않았으며 추가 기능 없이 상위 작업자 검토를 기다린다.
